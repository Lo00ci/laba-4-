#include "N.h"
#include <stdio.h>
#include <math.h>
int main() { 
    double a, b, c, x, y; 
    printf("a b c: ");
scanf("%lf%lf%lf", &a, &b, &c); 
if (a <= 0 || b <= 0
|| c <= 0 || a + b <= c || b + c <= a || c + a <= b)
printf("???????????? ?? ??????????");// ??????? ??????????? ?? ??????????
else {
    x = perimetr( a, b, c);
    y= ploshchad(a, b, c ); 
    printf("perimetr %g\n", x); 
    printf("ploshchad %g\n", y); 
    }
    
}


